{% extends 'base.html.twig' %}

{% block title %}Modifier un événement{% endblock %}

{% block body %}
 <section class="my-events-banner" style = "background-image: url('{{ asset('/uploads/' ~ event.image) }}')">
        <h1 class="my-events-title"><strong>MODIFIER l'ÉVÉNEMENT</strong></h1>
  </section> 
  <a class="user-button event-image-edit" id= "editBanner" href="{{ path('event_edit_banner', {'id': event.id}) }}">Modifier la bannière</a>
      {{ include('event/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
     <div class = "button-container">
      {{ include('event/_delete_form.html.twig') }}
     </div>
{% endblock %}
